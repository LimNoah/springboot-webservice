spring:
  profiles:
    include: real-oauth
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
  session:
    store-type: jdbc
